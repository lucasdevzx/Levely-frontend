function Skeleton({ className = "" }) {



    return (
        <div
            className={`component-background shimmer-diagonal rounded-lg ${className}`}
        />
    )
}

export default Skeleton